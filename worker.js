export default { async fetch(request, env) { const url = new URL(request.url); const cors = { "Access-Control-Allow-Origin":"*", "Access-Control-Allow-Headers":"Content-Type", "Access-Control-Allow-Methods":"GET,POST,OPTIONS" }; if (request.method === "OPTIONS") return new Response("", { headers: cors }); if (url.pathname === "/api/ping") return json({ ok:true, model:"gpt-5.5" }, 200, cors); if (url.pathname !== "/api/assistant" || request.method !== "POST") return json({ error:"Not found" }, 404, cors); const body = await request.json(); const system = "你是个人工作台任务提炼助手。只返回 JSON，不要 Markdown。格式：{\"tasks\":[{\"title\":\"任务名\",\"what\":\"明确动作\",\"why\":\"为什么现在做\",\"dueDate\":\"YYYY-MM-DD 或空\",\"priority\":\"high|medium|low\",\"hospital\":\"\",\"department\":\"\",\"contact\":\"\",\"project\":\"\",\"script\":\"建议话术\",\"detail\":\"\"}]}。最多 5 条；一句话中的多个动作必须拆成独立任务；没有明确日期时给合理建议日期；不要重复相同任务。"; const response = await fetch("https://api.302.ai/v1/chat/completions", { method:"POST", headers:{ "Authorization":`Bearer ${env.API_KEY}`, "Content-Type":"application/json" }, body:JSON.stringify({ model:"gpt-5.5", temperature:.2, messages:[{role:"system",content:system},{role:"user",content:JSON.stringify({message:body.message||"",context:body.context||{}})}] }) }); const raw = await response.json(); let result = { tasks:[] }; try { result = JSON.parse(raw.choices?.[0]?.message?.content || "{}"); } catch {} return json(result, response.status, cors); } };
function json(data, status, headers) { return new Response(JSON.stringify(data), { status, headers:{ ...headers, "Content-Type":"application/json; charset=utf-8" } }); }
