Personal Workbench V0.6.5 - Follow-up Review Center

The future radar groups overdue, today's, and upcoming follow-up items, and now includes a weekly review of completed work, stale contacts, and opportunities expected within 30 days. Existing localStorage data remains compatible.

Deploy index.html, app.js, style.css, manifest.json, sw.js, and worker.js to GitHub Pages. Keep the configured Cloudflare Worker URL. The Worker continues to use 302.AI and GPT-5.5; the API key stays in Worker secrets. Existing local records and backup fields are preserved.
